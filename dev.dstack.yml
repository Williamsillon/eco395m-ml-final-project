type: dev-environment
name: wildfire-chronos

python: "3.11"
ide: jupyter

setup:
  - pip install -r train/requirements.txt

resources:
  gpu:
    memory: 16GB..  # T4 or better; dstack Sky picks the cheapest available
